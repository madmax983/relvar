## 2025-04-06 - [Performance Optimization] Set Algebra Operators
**Learning:** Overly defensive relational algebra operators modifying existing collections (like `union_into`, `intersect_into`, `difference_into`) iterating, validating types per element and repeatedly hashing elements instead of directly utilizing underlying struct manipulations. Furthermore, assuming truncated file searches and method names before ensuring visibility.
**Action:** Exploit visibility mechanics like `pub(crate)` allowing modules inside the same crate to safely and effectively bypass redundant checks, enabling standard struct abstractions (such as HashSet's `reserve()`, `retain()` or `extend()`) directly. Make sure when planning changes all file elements targeted to change are completely retrieved via constrained sed views instead of blind guess.