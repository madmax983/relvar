## 2024-03-05 - Missing Tests in relvar-core/src/database/dml.rs
**Learning:** The DML utility functions `compute_relation_after_delete` and `compute_relation_after_update` in `relvar-core/src/database/dml.rs` lack direct unit testing.
**Action:** Add comprehensive unit tests verifying that deletions filter correctly, updates transform correctly and enforce type constraints, and both functions return accurate modification counts.
## 2024-03-05 - Missing Tests in relvar-core/src/database/dml.rs
**Learning:** The DML utility functions `compute_relation_after_delete` and `compute_relation_after_update` in `relvar-core/src/database/dml.rs` lack direct unit testing.
**Action:** Add comprehensive unit tests verifying that deletions filter correctly, updates transform correctly and enforce type constraints, and both functions return accurate modification counts.
