📖 Chapter: Documented the `Spreadsheet` module in `relvar/src/experimental/spreadsheet.rs`.
🔦 Insight: The experimental `Spreadsheet` engine was missing executable doc-tests, making it confusing to understand how to construct the necessary values and formulas relations and how to execute the evaluation loop. Added explicit usage examples for `Spreadsheet`, `new` and `evaluate`.
🧪 Example: Added 3 executable doctests.
🖼️ Preview: (docs are verified via `cargo doc --open`)
