💡 **The Spark:** "I noticed we can compute reachability using transitive closure and set difference, which sounds exactly like a Mark-and-Sweep Garbage Collector."
🚀 **The Feature:** "Implemented `RelationalGC` in `src/experimental/garbage_collector.rs` which uses pure relational algebra to identify garbage nodes in a graph."
🔭 **The Potential:** "Demonstrates how relational engines can elegantly solve complex graph-based algorithmic problems declaratively without procedural traversal loops."
⚠️ **Risk:** "Low. Isolated in `src/experimental/garbage_collector.rs`."
