💡 **The Spark:** "Can we model memory management using purely relational algebra?"
🚀 **The Feature:** "Implemented `MarkAndSweepGC` using relational algebra (tclose, join, union, difference) to resolve reachability and identify garbage objects declaratively."
🔮 **The Potential:** "Demonstrates how graph connectivity problems like reachability in garbage collectors compile cleanly into native query execution plans."
⚠️ **Risk:** "Low. Isolated in `src/experimental/garbage_collector.rs` behind the `nova` feature flag."