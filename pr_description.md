Title: 🌟 Nova: Relational Graph Neural Network

💡 **The Spark:** "I noticed we calculate joins and aggregations but don't use them for machine learning inference on graphs."
🚀 **The Feature:** "Implemented `GraphNeuralNetwork` in the experimental module using pure relational algebra."
🔮 **The Potential:** "Could be used for node classification, link prediction, or general message passing algorithms directly inside the database."
⚠️ **Risk:** "Low. Isolated in `src/experimental/graph_neural_network.rs`."
