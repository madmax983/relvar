💡 **The Spark:** "We have basic ML algorithms like K-Means and Naive Bayes, but what about Reinforcement Learning? Can we express optimal control and the Bellman equation purely relationally?"
🚀 **The Feature:** "Implemented `MdpSolver` which uses Value Iteration to find optimal value functions for Markov Decision Processes, using only Relvar's native relational algebra (Join, Extend, Summarize)."
🔮 **The Potential:** "Demonstrates that Relvar can serve as an inference engine for planning and decision-making problems, enabling in-database reinforcement learning."
⚠️ **Risk:** "Low. Purely additive and isolated in `src/experimental/mdp.rs`."
