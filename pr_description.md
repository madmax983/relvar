📖 Chapter: The `Spreadsheet` module examples.
🔦 Insight: The experimental `Spreadsheet` had missing and placeholder executable doc-tests for the main struct and its methods (`new` and `evaluate`), making it unclear how to initialize the values and formulas relations and evaluate the spreadsheet. Added executable examples to clarify the usage.
🧪 Example: Added 3 executable doctests for `Spreadsheet`, `new`, and `evaluate`.
🖼️ Preview: Documentation examples now provide copy-pasteable usage for initializing and evaluating the relational spreadsheet.
