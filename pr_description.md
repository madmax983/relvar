📖 Chapter: The `Spreadsheet` module (experimental)
🔦 Insight: Added executable doc-tests to the experimental relational spreadsheet engine. Replaced placeholder examples with full, working code demonstrating how to configure the `values` and `formulas` relations and run the evaluation to fixpoint.
🧪 Example: Added 3 executable doc-tests (`Spreadsheet` struct, `Spreadsheet::new`, and `Spreadsheet::evaluate`).
🖼️ Preview: Docs now accurately reflect how the spreadsheet engine handles declarative evaluation of formula cells.
