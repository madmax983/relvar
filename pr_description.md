📖 Chapter: The `Spreadsheet` engine module.
🔦 Insight: Explained how to instantiate and evaluate the relational spreadsheet engine, replacing placeholder documentation with real examples. Also fixed a clippy `for_kv_map` lint in the `timeseries` module discovered during CI verification.
🧪 Example: Added 3 executable doctests covering struct instantiation, `new`, and `evaluate` for `Spreadsheet`.
🖼️ Preview: (Verified locally with cargo doc)
