💡 **The Spark:** "I noticed we can model complex ML models like K-Means and Neural Networks, but we didn't have a Decision Tree evaluator yet."
🚀 **The Feature:** "Implemented `DecisionTree` which evaluates a tree using pure relational joins and extensions, treating node splits and instance feature evaluation as a relational pipeline."
🔮 **The Potential:** "Could be used for in-database scoring and inference without exporting data to Python."
⚠️ **Risk:** "Low. Isolated in `src/experimental/decision_tree.rs`."
