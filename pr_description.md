Title: 🎻 Bard: [documentation update]

📖 Chapter: The `storage` module in `relvar-storage`.
🔦 Insight: Re-exported `PAGE_SIZE` to fix doctest failures (`E0432`).
🧪 Example: The build and all tests pass locally.
