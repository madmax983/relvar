📖 Chapter: The `Spreadsheet` module
🔦 Insight: Replaced placeholder examples with real, executable doc-tests. Restored public visibility to `experimental` and `tools` modules. Fixed clippy warnings in `timeseries.rs`.
🧪 Example: Added 2 executable doctests for `Spreadsheet::new` and `Spreadsheet::evaluate`.
