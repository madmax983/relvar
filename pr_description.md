Title: 🌟 Nova: Relational Decision Tree Inference

💡 **The Spark:** "I noticed we use relational algebra for machine learning algorithms like K-Means and Graph Neural Networks, but lack a tree-based prediction model!"

🚀 **The Feature:** "Implemented a purely relational `DecisionTree` inference engine."

🔮 **The Potential:** "Could be used for fast batch inference and data-native AI predictions directly on the database engine using joins and extensions."

⚠️ **Risk:** "Low. Isolated in `src/experimental/decision_tree.rs` behind experimental."
